# type(varible) = returns the variable type
import tkinter

tkinter.Tcl().eval('info patchlevel') -> returns the tkinter version
def calculate(*args) -> doesnt limit the type and number of parameters.
